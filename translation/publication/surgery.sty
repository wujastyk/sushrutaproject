\usepackage{../incremental_SS_Translation,  % all customizations in this file
    gitver}
\addbibresource{../incremental_SS_Translation.bib} % putting this here lets TeXstudio 
%find it.

% Stylistic choices specifically for this book:
% 
\usepackage{xltxtra} % for the XeLaTeX logo
\setsansfont{TeX Gyre Heros}

\usepackage{rotating}
% geometry
\usepackage[
   paperwidth=155mm, %6.1in    %193mm,
   paperheight= 235mm, %9.25in %260mm,
%   textwidth = 105mm,
%   textheight = 193mm,
   top = 20mm,
   bottom = 20mm,
   right = 20mm,
   left = 20mm,
   % includefoot,
%     showcrop,
 %   showframe
]
{geometry}

% fancyheader
\pagestyle{fancy}
\renewcommand{\sectionmark}[1]{\markboth{#1}{}} % set the \leftmark
\fancyhf{}
\fancyhead[R]{\textenglish{\thepage}} %predefined ()
\fancyhead[L]{\empty} % 1. sectionname
\fancyfoot[C]{\empty}
%
\DeclareFieldFormat{url}{%
    \ifhyperref
    {\textsc{url: }\url{#1}}
    %    {\href{#1}{\textsc{url}}}
    {\textsc{url: }\nolinkurl{#1}}} %DW bug here
%
\usepackage{../chapter-headings}
\setcounter{secnumdepth}{0}

% Get \chapter headings; this is a “book”
\indexsetup{level=\chapter*,
    noclearpage,
    % firstpagestyle=fancy
    othercode= %\footnotesize
    \newcommand{\lettergroup}[1]{\relax}
}

% Changes to the way URLs are cited in the bibliography
% Enable hot URLs for PDFs at Archive.org and Academia.edu 
\newif\ifBibHiddenURLs
%
\ifBibHiddenURLs 
\relax
\else
%
\DeclareFieldFormat{url}{%
    \ifhyperref
    {\href{#1}{#1}} % added a newline to help with long URLs in PDFs
    %else
    {\textsc{url: }\nolinkurl{#1}}} %DW bug here
%
\DeclareFieldFormat{eprint:url}{%
    \ifhyperref
    {\href{#1}{#1}} % added a newline to help with long URLs in PDFs
    %else
    {\nolinkurl{#1}}} %DW bug here
%
\DeclareFieldFormat{eprint:ark}{%
    \ifhyperref  
    {\href{https://n2t.net/#1}{\nolinkurl{https://n2t.net/#1}}}
    %else
  {\nolinkurl{#1}}}
%
%\DeclareFieldFormat{eprint:jstor}{%
%    \ifhyperref
%    {\href{http://www.jstor.org/stable/#1}{{\small JSTOR}}}
%    %else
%    {\nolinkurl{#1}}}
\DeclareFieldFormat{doi}{%
    \ifhyperref
    {\href{https://doi.org/#1}{\nolinkurl{https://doi.org/#1}}}
     %else
    {\nolinkurl{#1}}}
%
\fi
\BibHiddenURLsfalse

