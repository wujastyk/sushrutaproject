\usepackage{xelatex-generic-biblatex-indexing,
    booktabs, % better-looking tables
    wasysym, %for the blob symbols in the table
    CC-license-footer,
    %   coffeestains,
}
\hangfootnotes

\renewfontfamily\devanagarifont
[Script=Devanagari,
FakeStretch=1.05,
Mapping=RomDev, %prefer slightly to iast
]
{Murty Sanskrit}
%{Sahadeva}
%{Sanskrit  2003}

\usepackage[final=true]{hyperref} %final so it doesn't get turned off by 
%todonotes' "draft"

\usepackage[
text={Draft of \today\ for private study only},
color={[gray]{0.95}},
scale=.27]{draftwatermark}

% just print hyperlinked “URL” in the bibliography, not the long, ugly URL.
\DeclareFieldFormat{url}{%
    \ifhyperref
    % {\textsc{url: }\href{#1}{URL}}
    {\href{#1}{\textsc{url}}}
    {\textsc{url: }\nolinkurl{#1}}} %DW bug here
\DeclareFieldFormat{eprint:ark}{%
    %    \textsc{ark:}\space
    \ifhyperref  
    {\href{https://n2t.net/#1}{\nolinkurl{#1}}}
    {\nolinkurl{#1}}}


\setlist[enumerate]{noitemsep} % enumitem global setting for \items
\setlist[itemize]{noitemsep} 

%% Make a new environment for the translation with tweaked spacing.
%% The old-fashioned LaTeX way:
%\newenvironment{translation}
%    {\begin{itemize}[noitemsep,
%            left=0pt .. \parindent]}
%    {\end{itemize}}

%% Make a new environment for the translation with tweaked spacing.
%% Do it the enumitem way:
\newlist{translation}{enumerate}{2}
\setlist[translation]{%
    label=\textbullet,
    noitemsep,
    left=0pt .. \parindent}


\newcommand{\Su}[2]{#1
    \parencite[#2]{vulgate}}
% references to the Suśrutasaṃhitā
% #1 = canonicau
% #2 = page of Ācārya's third edition
% later, when we've got our own numbers for our edition, we'll add them 
% as the second argument.

\newcommand{\As}[2]{As #1 
    \citep[#2]{atha-1980}} % references to the Saṅgraha, 
% #1 = canonical
% #2 = page of Aṭhāvale's edition

\newcommand{\Ah}[2]{Ah #1 
    \citep[#2]{kunt-1902}} % references to the Saṅgraha, 
% #1 = canonical
% #2 = page of Kunte and Navare's edition

\newcommand{\Cakra}[2]{Cakrapāṇidatta on #1 
    \citep[#2]{acar-1939}} % references to the 1939 ed, 
% #1 = canonical
% #2 = page of edition

\newcommand{\Dalhana}[2]{Ḍalhaṇa on #1 
    \citep[#2]{vulgate}} % references to the 1938^2 ed, 
% #1 = canonical
% #2 = page of edition


\newenvironment{prose}{\relax }{\relax }

\renewenvironment{verse}{\relax }{\relax }

\newenvironment{bhavanti}{\relax }{\relax }

%
%\newcommand{\item}[1]{$^{#1}$}

%\newenvironment{translation}{\list }{\endlist }

\renewcommand{\SS}{\emph{Suśrutasaṃhitā}}

\title{A Translation of the New Edition of the \SS}
\author{Jason Birch and Dominik Wujastyk}
\date{\texttt{Draft of \today}\\ \copyright\ Jason Birch and Dominik Wujastyk}

\let\se\saneng  % just make typing a bit quicker
\let\sed\sanengdev % puts the Skt. in the text into Devanagari.

\def\root{\ifmmode\surd\else$\surd$\fi } % make it easier to say \root bhū

%\usepackage[colorinlistoftodos,
%%    disable,
%textsize=tiny,
%shadow,
%backgroundcolor=yellow]{todonotes}
%%
%\def\q#1{{}\todo{#1}{}}
%%Put a \verb|\newpage| \verb|\listoftodos| just before \verb|\end{document}|

% \q{} is now defined in xelatex-generic.sty

